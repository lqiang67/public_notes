# Ignore everything by default
*

# Allow README.md
!README.md

# Allow index.html for GitHub Pages
!index.html

# Allow PDF files anywhere
!*.pdf
!**/*.pdf

# Allow HTML files for PDF viewers
!*.html
!**/*.html

# Allow directories (needed to access PDFs in subdirectories)
!*/
!.gitignore

# Allow .title files for manual titles
!.title
!**/.title

# Ignore scheduled items directory
.scheduled/

# Ignore notes mapping file (internal tracking)
.notes_mapping.json
